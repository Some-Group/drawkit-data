{
  "name": "@some-group/drawkit-data",
  "version": "1.0.0",
  "description": "Drawkit data structure",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "npm run build:schemas ./schemas ./src && npm run build:pkg",
    "build:schemas": "ts-node ./scripts/build.ts",
    "build:pkg": "rm -rf dist && mkdir -p dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": ["dist"],
  "keywords": ["json", "schema"],
  "author": "Boiao Ch <doubiliao@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/node": "^18.0.6",
    "chalk": "^4.1.2",
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
